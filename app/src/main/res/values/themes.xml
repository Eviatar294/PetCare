<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme with ActionBar -->
    <style name="Base.Theme.PetCare" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme colors here -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/colorAccent</item>
    </style>

    <!-- The app theme -->
    <style name="Theme.PetCare" parent="Base.Theme.PetCare" />
</resources>
